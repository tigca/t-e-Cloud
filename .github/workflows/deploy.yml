name: main
on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  deploy:
      - name: Build step
        run: "pip install pyTelegramBotAPI" # 📝 Update the build command(s) if necessary

project: "fair-toad-60"
          entrypoint: "app.py" # 📝 Update the entrypoint if necessary
          root: "." # 📝 Update the root if necessary
